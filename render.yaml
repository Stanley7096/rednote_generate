services:
  - type: web
    name: xhs-note-gen
    env: node
    plan: free
    buildCommand: "npm install && npx playwright install --with-deps chromium"
    startCommand: "node server.js"
    envVars:
      - key: NODE_ENV
        value: production
